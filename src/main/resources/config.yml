arkitektonika:
  # The url of the backend server (Arkitektonika)
  backendUrl: https://api.voxisle.art/tools/arkitektonika
  # The url used to generate a download link from.
  downloadUrl: https://api.voxisle.art/tools/arkitektonika/download/{key}
  # The url used to generate a deletion link from.
  deleteUrl: https://api.voxisle.art/tools/arkitektonika/delete/{key}
web:
  # The url of the frontend
  frontend: https://schem.alsace.team
  # The url used to generate a download link from.
  downloadUrl: https://api.voxisle.art/tools/arkitektonika/download/{key}
  # The url used to generate a deletion link from.
  deleteUrl: https://api.voxisle.art/tools/arkitektonika/delete/{key}
